# explore tag basic concept of Git

## To initiazation the git use this command

```bash 
git add .
git commit -m "Proper_message_name"
```

## To rename recent branches

```bash
git branch -M main
```